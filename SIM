#!/usr/bin/env python3

import sys
import war
import trash

type = sys.argv[1]
file = sys.argv[2]
if type == "war":
	war.start(file)
elif type == "trash":
	trash.start(file)
else:
	print("Enter a valid game type (war or trash)!")
